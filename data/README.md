# Local datasets

This directory contains local training data and is excluded from Git.

Expected roots:

- `detection/AOD_4/` — YOLO images and labels for train/validation splits.
- `classification/` — ImageFolder-compatible train/validation splits.

Do not commit dataset images or labels. Add public download links to the main README when available.
